services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: geojero
      POSTGRES_USER: geojero
      POSTGRES_PASSWORD: geojero
    ports:
      - "5432:5432"
    volumes:
      - pg_local:/var/lib/postgresql/data

volumes:
  pg_local: