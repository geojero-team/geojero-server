spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/geojero-server
    username: geojero
    password: geojero
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true