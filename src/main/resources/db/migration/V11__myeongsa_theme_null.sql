-- 명사해수욕장을 화면 목록에서 내린다 (2026-09-11 팀 결정).
--
-- V4가 정한 계약: "화면은 theme 이 NULL 인 POI 를 목록에서 거른다."
-- 그런데 V6이 명사해수욕장을 시드하면서 theme='BEACH'를 함께 넣어, theme != null 이 9건이
-- 되었다. 화면이 말하는 스팟은 8곳인데 서버는 9곳을 내려주는 상태였다.
--
-- 왜 내리는가 — 기준문서 §6 「스팟 8곳 선정 기준」이 세 이유를 든다:
--   1. 거제시 공식 「거제 9경」이 아니다 (스팟 선정의 기준 자체)
--   2. 53·53-1이 도로 유실로 명사해수욕장앞을 우회 중이라 코스가 성립하지 않는다
--   3. TourAPI 사진이 전 장 Type3라 화면에 쓸 그림이 없다
-- 성립을 보여줄 수 없는 스팟은 이 서비스에서 가치가 없다.
--
-- **행은 지우지 않는다.** contentId·좌표·photo_keyword는 TourAPI 실호출로 확인한 값이고
-- (V6·V8), 우회가 해제되면 그대로 되살려야 한다(기준문서 §9 미해결 항목).
-- 지우면 다시 조사해야 한다. 화면 분류만 거두어 목록에서 빠지게 한다.

UPDATE pois SET theme = NULL WHERE poi_name = '명사해수욕장';
